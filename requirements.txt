# Core dependencies for Luno Trading Bot
requests>=2.28.0
python-dotenv>=0.19.0

# Data analysis and technical indicators
pandas>=1.3.0
finta>=1.0.0

# Web framework (for dashboard - optional for Railway bot)
flask>=2.0.0
SQLAlchemy>=1.4.0

# Testing (optional for deployment)
pytest>=7.0.0
pytest-cov>=4.0.0

# Process management (for auto-sell monitor)
psutil>=5.9.0

# Production server (optional - can use gunicorn)
gunicorn>=20.1.0

# Procfile manager for Docker and local multi-process runs
honcho>=1.1.0

# Additional utilities
pytz>=2022.0
